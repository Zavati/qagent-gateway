# Foundation 07.7.10-A — Gateway Suite/Inventory BFF

Adds authorized Console BFF routes for Project Test Inventory and automatic Suite materialization/latest retrieval.

The browser never accesses Test Registry directly. No Gateway migration is introduced.
